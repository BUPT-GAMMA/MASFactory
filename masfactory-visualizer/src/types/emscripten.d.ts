// Minimal global typing shim for web-tree-sitter's `Parser.init(moduleOptions?: EmscriptenModule)`.
// We intentionally avoid pulling in additional type dependencies for Emscripten.
declare type EmscriptenModule = any;

